# Examples

Nothing custom yet. The MVP is the agent itself:

```bash
pixi run mu --fake -p "hello"
pixi run mu -p "explain this repo"
```

Later this folder will hold extra tools, prompt templates, and frontend sketches.
